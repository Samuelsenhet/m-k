diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 1467a39..2b5a949 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -186,7 +186,16 @@ function getInitialURLWithTimeout() {
     }
     else if (react_native_1.Platform.OS === 'ios') {
         // Use the new Expo API for iOS. This has better support for App Clips and handoff.
-        return ExpoLinking.getLinkingURL();
+        // Fallback when native ExpoLinking.getLinkingURL is missing (stale dev client / pod mismatch).
+        try {
+            return ExpoLinking.getLinkingURL();
+        }
+        catch (_e) {
+            return Promise.race([
+                react_native_1.Linking.getInitialURL(),
+                new Promise((resolve) => setTimeout(() => resolve(null), 150)),
+            ]);
+        }
     }
     return Promise.race([
         // TODO: Phase this out in favor of expo-linking on Android.
