/**
 * MÄÄK screen layout – single source of truth for all app screens.
 * Use this container class so spacing and width are consistent.
 */
export const SCREEN_CONTAINER_CLASS =
  "container max-w-2xl mx-auto px-4 pt-6 pb-24";

/** Same width/padding as screen container, no vertical rhythm (e.g. for landing sections). */
export const SCREEN_CONTENT_WIDTH_CLASS = "max-w-2xl mx-auto px-4";
