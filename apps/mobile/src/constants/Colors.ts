const tint = "#2e78b7";

/**
 * Minimal color palette for mobile template components.
 * Only `Colors.light.tint` is currently used in the codebase.
 */
const Colors = {
  light: {
    text: "#111827",
    background: "#ffffff",
    tint,
  },
  dark: {
    text: "#f9fafb",
    background: "#000000",
    tint,
  },
};

export default Colors;

