# Internal / preview distribution (root + apps/mobile eas.json → profile "expo-preview").
# Manual: cd apps/mobile && npx eas-cli@latest workflow:run .eas/workflows/preview-builds.yml
name: Create preview build (iOS only)

jobs:
  build_ios:
    type: build
    params:
      platform: ios
      profile: expo-preview
